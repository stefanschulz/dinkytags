<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="DinkyTags">
	<title>DinkyTags</title>
	<!-- Reduced from logo.svg for small sizes (favicon, JED listing thumbnail): the
	     near-flat label with its point to the top-right and the punched hole survive
	     at 16px; the < > cut into the body does not, so it is dropped. currentColor so
	     it recolours with its context. -->
	<g transform="translate(12,12) rotate(-9) scale(1.34)">
		<path d="M8 0 L4 -6 L-6.5 -6 Q-8.5 -6 -8.5 -4 L-8.5 4 Q-8.5 6 -6.5 6 L4 6 Z"/>
		<circle cx="4.2" cy="0" r="1.5" fill="currentColor" stroke="none"/>
	</g>
</svg>
